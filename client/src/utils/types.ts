export type ISideBarRoutes = {
    routePath: string,
    routeName: string,
    routeElement: JSX.Element
} 

export type IFilterButtons = {
    btnComponent: JSX.Element,
    btnTitle: string
}

export type IDashboardOrientation = {
    btnValue: number,
    btnTitle: string
}